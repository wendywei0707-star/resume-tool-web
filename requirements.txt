# ============================================================================
# 智能简历匹配与改写工具（网页版）- Python 依赖
# ============================================================================

# Web 应用框架
streamlit>=1.28.0

# Word 文档处理
python-docx>=0.8.11

# PDF 处理（读取 + reportlab 兜底转换）
PyMuPDF>=1.23.0
reportlab>=4.0.0

# 图片处理
Pillow>=10.0.0

# OCR 文字识别（图片提取文字，云端需配合 packages.txt 里的 tesseract-ocr）
pytesseract>=0.3.10

# 中文分词（岗位分析）
jieba>=0.42.1

# LLM API 调用（智能简历改写，可选，用户自行输入 Key 才会用到）
openai>=1.0.0
